version: "3.9"

services:
  # OpenMind CLI (interactive)
  openmind:
    build: .
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
      - openmind-data:/root/.openmind
    profiles:
      - cli

  # Telegram Bot
  bot:
    build: .
    entrypoint: ["python", "bot/telegram_bot.py"]
    env_file: .env
    restart: unless-stopped
    volumes:
      - openmind-data:/root/.openmind
    profiles:
      - bot

  # Landing Page (nginx)
  landing:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./landing:/usr/share/nginx/html:ro
    profiles:
      - web

volumes:
  openmind-data:
